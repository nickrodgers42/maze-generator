'use strict';

import Maze from "./maze.js";

let main = new p5((sketch) => {
    sketch.preload = () => {
    }

    sketch.setup = () => {
        const canvas = sketch.createCanvas(500, 500);
        const maze = new Maze();
    :

    sketch.draw = () => {
    }
});



